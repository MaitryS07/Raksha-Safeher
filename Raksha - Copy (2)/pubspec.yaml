name: raksha
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  url_launcher: ^6.3.0
  http: ^1.6.0
  permission_handler: ^11.3.1
  sensors_plus: ^7.0.0
  shared_preferences: ^2.3.2
  geolocator: ^14.0.2
  provider: ^6.1.2
  speech_to_text: ^7.3.0
  flutter_background_service: ^5.1.0
  vibration: ^3.1.5

  sqflite: ^2.4.2
  path: ^1.9.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/models/distress_classifier.onnx
